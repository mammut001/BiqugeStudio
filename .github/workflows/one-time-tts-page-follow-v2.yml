name: One-time TTS page-follow patch v2

"on":
  push:
    branches:
      - fix/tts-cross-page-follow
    paths:
      - .github/workflows/one-time-tts-page-follow-v2.yml

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/tts-cross-page-follow
          fetch-depth: 0

      - name: Apply TTS page-follow patch
        run: python3 .github/tts_page_follow_patch.py

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Validate patched tree
        run: |
          chmod +x gradlew
          ./gradlew :app:testDebugUnitTest :app:lint :app:assembleDebug --stacktrace --no-daemon

      - name: Commit patch and remove temporary files
        run: |
          rm .github/tts_page_follow_patch.py
          rm .github/workflows/one-time-tts-page-follow.yml
          rm .github/workflows/one-time-tts-page-follow-v2.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- app/src/main/java/app/maoyankanshu/novel/selfuse/ui/reader/ReaderTtsController.kt app/src/main/java/app/maoyankanshu/novel/selfuse/ui/reader/ReaderScreen.kt .github/tts_page_follow_patch.py .github/workflows/one-time-tts-page-follow.yml .github/workflows/one-time-tts-page-follow-v2.yml
          git commit -m 'feat(reader): follow TTS across page boundaries'
          git push origin HEAD:fix/tts-cross-page-follow
